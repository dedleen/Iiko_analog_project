<Page x:Class="Kvalif.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Kvalif.Converters"
    mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderPage">
    <Page.Resources>
        <local:BooleanToServedConverter x:Key="BooleanToServedConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

      
        <StackPanel Grid.Column="0" Margin="10">
            <ListBox x:Name="DishesListBox" Height="400" ItemsSource="{Binding OrderItems}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DishName}" Width="150"/>
                            <TextBlock Text=" - " Width="10"/>
                            <TextBlock Text="{Binding Price}" Width="50" HorizontalAlignment="Right"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="DeleteButton" Content="Удалить" Click="DeleteButton_Click" Width="60"/>
                                <Button x:Name="AcceptButton" Content="{Binding IsServed, Converter={StaticResource BooleanToServedConverter}}" Click="AcceptButton_Click" Width="60"/>
                                <TextBlock Text="Время" Width="50"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Background="LightGray" Height="50" Margin="0,10,0,0">
                <TextBlock Text="Итог:" HorizontalAlignment="Right" Margin="0,10,10,0"/>
                <TextBlock x:Name="TotalPrice" Text="0.00 Р" FontSize="16" HorizontalAlignment="Right" Margin="0,0,10,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Правая колонка: Категории и поиск -->
        <StackPanel Grid.Column="2" Margin="10">
            <TextBox x:Name="Find" Margin="0,0,0,10" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</Page>